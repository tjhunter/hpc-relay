[project]
name = "hpc-relay"
version = "0.1.0"
description = "Durable Python orchestration for commands and Slurm jobs across EuroHPC systems."
requires-python = ">=3.12"
dependencies = [
    "prefect>=3.7.0",
    # Prefect 3.7.0 is not compatible with the FastAPI 0.135 / Starlette 1.0
    # routing rewrite (its PrefectRouter relies on internals those versions
    # removed, causing "'PrefectRouter' object has no attribute 'routes'" on
    # every API request). Pin to the last known-good range until Prefect
    # supports the new stack.
    "fastapi<0.116",
    "starlette<0.42",
    "paramiko>=3.1",
    "pyfirecrest>=3.7",
    "dacite",
    "pyunicore>=1.3.7",
]

[dependency-groups]
dev = [
 "pytest>=8.3",
 "pytest-mock>=3.14.1",
 "ruff==0.9.7",
 "pyrefly==0.36.0",
]


[tool.basedpyright]
# Do not require third-party/internal packages to ship type stub files.
reportMissingTypeStubs = false
# Allow `from ... import *` (used in example scripts for convenience).
reportWildcardImportFromLibrary = false

[tool.pyrefly]
project-includes = ["src/"]
project-excludes = [
]

[tool.pyrefly.errors]
# bad-argument-type = true
# unsupported-operation = false
# missing-attribute = false
# no-matching-overload = false
# bad-context-manager = false




# The linting configuration
[tool.ruff]

# Wide rows
line-length = 100

[tool.ruff.lint]
# All disabled until the code is formatted.
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # isort
    "I",
    # Banned imports
    "TID",
    # Naming conventions
    "N",
    # print
    "T201"
]

# These rules are sensible and should be enabled at a later stage.
ignore = [
  # "B006",
  "B011",
  "UP008",
  "SIM117",
  "SIM118",
  "SIM102",
  "SIM401",
  # To ignore, not relevant for us
  "SIM108", # in case additional norm layer supports are added in future
  "N817", # we use heavy acronyms, e.g., allowing 'import LongModuleName as LMN' (LMN is accepted)
  "E731", # overly restrictive and less readable code
  "N812", # prevents us following the convention for importing torch.nn.functional as F
]

[tool.ruff.lint.per-file-ignores]
# Examples are illustrative scripts: long URLs/comments and print() are fine.
"examples/**" = ["E501", "T201"]

[tool.ruff.lint.flake8-tidy-imports.banned-api]
"numpy.ndarray".msg = "Do not use 'ndarray' to describe a numpy array type, it is a function. Use numpy.typing.NDArray or numpy.typing.NDArray[np.float32] for example"

[tool.ruff.format]
# Use Unix `\n` line endings for all files
line-ending = "lf"

[tool.prefect]
# Show DEBUG-level logs from Prefect itself
logging.level = "INFO"

logging.extra_loggers = "hpc_relay"


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/"]
